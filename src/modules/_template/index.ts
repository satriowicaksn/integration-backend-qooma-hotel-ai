// Barrel export — public API modul. TIDAK export repository/adapter (internal).
//
// ★ FOLDER _template/ ADALAH REFERENSI POLA — JANGAN DIEDIT.
// Saat buat modul baru: `cp -r src/modules/_template src/modules/<new-name>`
// lalu rename file `_template.*` → `<new-name>.*`.
// Lihat docs/MODULE_TEMPLATE.md untuk panduan lengkap.

export {};
