name: Deploy to VPS staging

on:
  push:
    branches: [main]
    paths:
      - 'src/**'
      - 'prisma/**'
      - 'Dockerfile'
      - 'deploy/k8s/integration/**'
      - '.github/workflows/deploy-staging.yml'
  workflow_dispatch:

concurrency:
  group: deploy-staging
  cancel-in-progress: false

env:
  REGISTRY: ghcr.io
  IMAGE_API: ghcr.io/satriowicaksn/integration-backend-api
  IMAGE_WORKER: ghcr.io/satriowicaksn/integration-backend-worker

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set image tags
        id: tags
        run: |
          echo "sha=main-${GITHUB_SHA::12}" >> "$GITHUB_OUTPUT"
          echo "date=main-$(date -u +%Y-%m-%d)" >> "$GITHUB_OUTPUT"

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push api image
        uses: docker/build-push-action@v6
        with:
          context: .
          target: api
          push: true
          tags: |
            ${{ env.IMAGE_API }}:latest
            ${{ env.IMAGE_API }}:${{ steps.tags.outputs.sha }}
            ${{ env.IMAGE_API }}:${{ steps.tags.outputs.date }}
          cache-from: type=gha,scope=api
          cache-to: type=gha,mode=max,scope=api

      - name: Build and push worker image
        uses: docker/build-push-action@v6
        with:
          context: .
          target: worker
          push: true
          tags: |
            ${{ env.IMAGE_WORKER }}:latest
            ${{ env.IMAGE_WORKER }}:${{ steps.tags.outputs.sha }}
            ${{ env.IMAGE_WORKER }}:${{ steps.tags.outputs.date }}
          cache-from: type=gha,scope=worker
          cache-to: type=gha,mode=max,scope=worker

      - name: Configure kubectl
        env:
          KUBECONFIG_B64: ${{ secrets.VPS_KUBECONFIG_B64 }}
        run: |
          mkdir -p "$HOME/.kube"
          echo "$KUBECONFIG_B64" | base64 -d > "$HOME/.kube/config"
          chmod 600 "$HOME/.kube/config"
          kubectl version --client=true

      - name: Run Prisma migration Job
        run: |
          # Delete any old completed Job so we can re-create with the new image.
          kubectl -n integration-staging delete job integration-migrate --ignore-not-found=true
          kubectl apply -f deploy/k8s/integration/job-migrate.yaml
          kubectl -n integration-staging wait --for=condition=complete job/integration-migrate --timeout=240s
          kubectl -n integration-staging logs job/integration-migrate

      - name: Apply manifests
        run: |
          kubectl apply -f deploy/k8s/integration/namespace.yaml
          kubectl apply -f deploy/k8s/integration/configmap.yaml
          kubectl apply -f deploy/k8s/integration/service.yaml
          kubectl apply -f deploy/k8s/integration/ingress.yaml

      - name: Roll api + worker to the new image
        env:
          SHA_TAG: ${{ steps.tags.outputs.sha }}
        run: |
          kubectl -n integration-staging set image \
            deployment/integration-api \
            api="${IMAGE_API}:${SHA_TAG}"
          kubectl -n integration-staging set image \
            deployment/integration-worker \
            worker="${IMAGE_WORKER}:${SHA_TAG}"

          # Ensure the Deployment specs exist (first-run apply).
          kubectl apply -f deploy/k8s/integration/deployment.yaml

          kubectl -n integration-staging rollout status deployment/integration-api --timeout=240s
          kubectl -n integration-staging rollout status deployment/integration-worker --timeout=240s

      - name: Smoke test
        run: |
          for _ in $(seq 1 30); do
            if curl -fsS https://integration-staging.sharedisini.com/healthz; then
              echo
              echo "Smoke test OK."
              exit 0
            fi
            echo "Waiting for healthz..."
            sleep 4
          done
          echo "Smoke test failed after 2 minutes."
          exit 1
